{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mistakia.github.io/react-table/schema/state/sort-item.json",
  "title": "SortItem",
  "type": "object",
  "properties": {
    "column_id": {
      "type": "string",
      "description": "ID of the column to sort by"
    },
    "desc": {
      "type": "boolean",
      "description": "Whether to sort in descending order"
    },
    "column_index": {
      "type": "integer",
      "description": "Index of the column instance (for duplicate column IDs)",
      "default": 0
    },
    "multi": {
      "type": "boolean",
      "description": "Whether this is part of multi-column sort"
    }
  },
  "required": ["column_id", "desc"]
}
